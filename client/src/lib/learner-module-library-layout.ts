/**
 * Single source of truth for the open learner module library strip height.
 * Keep in sync: strip root `h-[…]` and `documentElement` `--lml-strip-offset` in canvas-view.
 */
export const LML_STRIP_HEIGHT_CLAMP = "clamp(15rem, 32vh, 25rem)";
