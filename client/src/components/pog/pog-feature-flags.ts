/**
 * When false (v1 PoG): hide outcome linking UI, scores, “where built,” “Start with outcomes,”
 * and skip syncing portrait links into Key Design targeted outcomes. On load, portrait outcome
 * links and `source: "pog"` center aims are cleared. Set to `true` to restore full behavior
 * (data may need to be re-linked).
 */
export const POG_SHOW_OUTCOME_LINKING_AND_ADVANCED_UI = false;
